-- Baseline migration
-- This migration marks the current database state as the baseline
-- No changes needed - database already has all tables

-- Just a marker migration to reset migration history
SELECT 1;
